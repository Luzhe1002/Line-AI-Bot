# Git 操作規則

- 在修改、暫存、提交及推送前，核對目前分支、上游分支、工作區狀態及本次功能範圍。
- 同一功能的後續改善、介面調整、修錯及驗證，預設沿用尚未合併的原功能分支；不要每一輪對話或每一項修改都另開分支。
- 只有獨立的新任務才建立名稱相符的分支。先查是否已有對應功能分支及其合併狀態；不要因工作區目前停在某分支就沿用無關分支，也不要重複建立同功能分支。
- 選擇基底時檢查相對目標分支的提交與差異，避免混入其他尚未合併的功能。
- 若原功能分支尚未合併，而新任務要基於它另開分支、修改與它重疊的檔案，或可能造成重複提交與合併順序依賴，必須在建立新分支前先提醒使用者：原分支、合併狀態、擬用基底、已知重疊及可能的衝突。不把可能衝突說成必然；優先建議沿用原分支或先合併。需要建立有依賴的新分支時，取得使用者明確同意後才進行。
- 合併重複的功能分支時，先檢查祖先關係；能快轉就使用 --ff-only，避免不必要的 cherry-pick 或 revert。確認所有提交已保留且目標遠端同步後，才清理使用者授權移除的多餘分支。
- 拆分、撤回或重新選擇部署分支時，核對已部署的資料庫遷移歷史。已套用的 Flyway 遷移必須原樣保留，即使相關應用功能不再包含於該分支；不得以刪除遷移、repair 或停用驗證代替相容性處理。
- 暫存明確列出的檔案，檢查 staged diff；保留與本次任務無關的使用者修改及暫存資料。
- 推送前確認目標遠端與分支名稱；推送後核對遠端提交與本機提交一致。
- 整理已推送的分支優先以 revert 保留歷史。改寫共享歷史前須有明確授權，並使用帶預期提交的 force-with-lease。
- 回報實際分支、提交及驗證結果；未合併或未部署不得描述為已上線。

# 正式發布規則

- 本專案發布、部署、回復與分支管理使用 `.agents/skills/line-ai-release/SKILL.md`，並讀取 `docs/production-release.md`。
- 功能 PR 進 main；正式發布只由 main PR 進 production，以 merge commit 合併。production 禁止直接推送、force push 或刪除。
- 正式 Render 使用 production 且 CI 通過後部署；切換前必須核對實際部署 commit 與 Flyway 歷史。render.yaml 修改不等於服務已切換。
